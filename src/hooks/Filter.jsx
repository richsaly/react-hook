// import { useState, useEffect } from 'react';




// function useFilter(dataArray, filterWord) {
  
//   const [loading, setLoading] = useState(true);

  
//   const filteredData = dataArray.filter(item => item === filterWord);

//  useEffect(() => {
//   setTimeout(() => {
//       setLoading(false); 
//     }, 2000);
   
//  }, [loading])
 

//   return {filteredData, loading};
// }

// export default useFilter;



import { useState, useEffect } from 'react';

const useFilter = (arrData, filterWords) => {
  const [filteredData, setFilteredData] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const filterArray = () => {
      if (filterWords.length === 0) {
        setFilteredData(arrData);
      } else {
        const filtered = arrData.filter(item =>
          filterWords.some(word => item.includes(word))
        );
        setFilteredData(filtered);
      }
      setLoading(false);
    };
    filterArray();
  }, [arrData, filterWords]);

  return { filteredData, loading };
};

export default useFilter;




















































































































































































































































































































































































































































































































